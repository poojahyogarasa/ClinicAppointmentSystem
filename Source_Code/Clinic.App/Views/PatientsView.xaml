<UserControl x:Class="Clinic.App.Views.PatientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="920" d:DesignHeight="520">

    <!-- Local resources to prevent missing style errors -->
    <UserControl.Resources>
        <!-- Card style -->
        <SolidColorBrush x:Key="CardBg" Color="#F7FAFF"/>
        <SolidColorBrush x:Key="CardBorder" Color="#D9E6FF"/>
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,16,16"/>
        </Style>

        <!-- Label style -->
        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="Label" TargetType="TextBlock" BasedOn="{StaticResource FormLabel}" />
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.5*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Side: Patient Form -->
        <Border Style="{StaticResource Card}" Grid.Column="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Name -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Name" Style="{StaticResource Label}"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="200"/>

                <!-- Phone -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Phone" Style="{StaticResource Label}"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" Width="200"/>

                <!-- Email -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Email" Style="{StaticResource Label}"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Width="200"/>

                <!-- Address -->
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Address" Style="{StaticResource Label}"/>
                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" Width="200"/>

                <!-- Buttons -->
                <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,12,0,0">
                    <Button Content="Save"   Command="{Binding AddCommand}"    Width="70" Margin="0,0,10,0"/>
                    <Button Content="Update" Command="{Binding UpdateCommand}" Width="70" Margin="0,0,10,0"/>
                    <Button Content="Delete" Command="{Binding DeleteCommand}" Width="70" Margin="0,0,10,0"/>
                    <Button Content="Clear"  Command="{Binding ClearCommand}"  Width="70"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Right Side: DataGrid -->
        <Border Style="{StaticResource Card}" Grid.Column="1">
            <DataGrid AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      ItemsSource="{Binding Patients}"
                      SelectedItem="{Binding Selected, Mode=TwoWay}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"      Binding="{Binding Id}"      Width="60"/>
                    <DataGridTextColumn Header="Name"    Binding="{Binding Name}"    Width="*"/>
                    <DataGridTextColumn Header="Phone"   Binding="{Binding Phone}"   Width="*"/>
                    <DataGridTextColumn Header="Email"   Binding="{Binding Email}"   Width="*"/>
                    <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
