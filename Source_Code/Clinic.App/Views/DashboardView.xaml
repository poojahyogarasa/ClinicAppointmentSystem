<UserControl x:Class="Clinic.App.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="920" d:DesignHeight="520">

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="16"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left metrics -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Style="{StaticResource Card}" Grid.Row="0">
                <DockPanel>
                    <TextBlock Text="Total Doctors" FontSize="16" DockPanel.Dock="Top"/>
                    <TextBlock Text="{Binding DoctorCount}" FontSize="40" FontWeight="Bold" Margin="0,6,0,0"/>
                </DockPanel>
            </Border>

            <Border Style="{StaticResource Card}" Grid.Row="1">
                <DockPanel>
                    <TextBlock Text="Total Patients" FontSize="16" DockPanel.Dock="Top"/>
                    <TextBlock Text="{Binding PatientCount}" FontSize="40" FontWeight="Bold" Margin="0,6,0,0"/>
                </DockPanel>
            </Border>

            <Border Style="{StaticResource Card}" Grid.Row="2">
                <DockPanel>
                    <TextBlock Text="Appointments Today" FontSize="16" DockPanel.Dock="Top"/>
                    <TextBlock Text="{Binding TodayCount}" FontSize="40" FontWeight="Bold" Margin="0,6,0,0"/>
                </DockPanel>
            </Border>

            <Border Style="{StaticResource Card}" Grid.Row="3">
                <StackPanel>
                    <TextBlock Text="Next Appointment" FontSize="16"/>
                    <TextBlock Text="{Binding NextAppointmentText}" FontSize="18" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Right welcome panel -->
        <Border Grid.Column="2" Style="{StaticResource Card}">
            <TextBlock Text="Welcome! Select a module from the left."
                 HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Border>
    </Grid>
</UserControl>
