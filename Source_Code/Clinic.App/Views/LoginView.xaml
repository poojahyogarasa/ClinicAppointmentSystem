<UserControl x:Class="Clinic.App.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:helpers="clr-namespace:Clinic.App.Helpers"
             mc:Ignorable="d"
             d:DesignWidth="780" d:DesignHeight="460">
    <Grid Background="#F7F9FC" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Login"
                   FontSize="24"
                   FontWeight="SemiBold" />

        <StackPanel Grid.Row="2" VerticalAlignment="Top" Width="320">
            <TextBlock Text="Username" Margin="0,0,0,6"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Padding="8" />

            <TextBlock Text="Password" Margin="0,12,0,6"/>
            <PasswordBox
                helpers:PasswordBoxHelper.BindPassword="True"
                helpers:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                Padding="8" />

            <Button Content="Login"
                    Margin="0,14,0,0"
                    Padding="8,6"
                    Command="{Binding LoginCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
