<UserControl x:Class="Clinic.App.Views.DoctorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="920" d:DesignHeight="520">

    <!-- Local resources so this view never crashes on missing styles -->
    <UserControl.Resources>
        <!-- Card style -->
        <SolidColorBrush x:Key="CardBg" Color="#F7FAFF"/>
        <SolidColorBrush x:Key="CardBorder" Color="#D9E6FF"/>
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,16,16"/>
        </Style>

        <!-- Form label style -->
        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <!-- Compatibility alias so old {StaticResource Label} still resolves -->
        <Style x:Key="Label" TargetType="TextBlock" BasedOn="{StaticResource FormLabel}" />
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource Card}" Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="260"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="260"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <!-- add explicit rows for clean layout -->
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Labels + Inputs -->
                <TextBlock Text="Name"   Style="{StaticResource Label}" Grid.Column="0" Grid.Row="0"/>
                <TextBox   Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Grid.Row="0"/>

                <TextBlock Text="Email"  Style="{StaticResource Label}" Grid.Column="2" Grid.Row="0"/>
                <TextBox   Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Grid.Column="3" Grid.Row="0"/>

                <TextBlock Text="Phone"  Style="{StaticResource Label}" Grid.Column="0" Grid.Row="1"/>
                <TextBox   Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Grid.Row="1"/>

                <TextBlock Text="Specialty" Style="{StaticResource Label}" Grid.Column="2" Grid.Row="1"/>
                <TextBox   Text="{Binding Specialty, UpdateSourceTrigger=PropertyChanged}" Grid.Column="3" Grid.Row="1"/>

                <!-- Buttons -->
                <StackPanel Orientation="Horizontal" Grid.ColumnSpan="5" Grid.Row="2" Margin="0,14,0,0">
                    <Button Content="Add"    Command="{Binding AddCommand}"/>
                    <Button Content="Update" Command="{Binding UpdateCommand}"/>
                    <Button Content="Delete" Command="{Binding DeleteCommand}"/>
                    <Button Content="Clear"  Command="{Binding ClearCommand}"/>
                    <Button Content="Reload" Command="{Binding ReloadCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  ItemsSource="{Binding Doctors}"
                  SelectedItem="{Binding Selected, Mode=TwoWay}"
                  SelectionChanged="DataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"        Binding="{Binding Id}"        Width="60"/>
                <DataGridTextColumn Header="Name"      Binding="{Binding Name}"      Width="*"/>
                <DataGridTextColumn Header="Email"     Binding="{Binding Email}"     Width="*"/>
                <DataGridTextColumn Header="Phone"     Binding="{Binding Phone}"     Width="140"/>
                <DataGridTextColumn Header="Specialty" Binding="{Binding Specialty}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
