<UserControl x:Class="Clinic.App.Views.AppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="920" d:DesignHeight="520">

    <!-- Local resources so this file never depends on App.xaml styles -->
    <UserControl.Resources>
        <SolidColorBrush x:Key="CardBg" Color="#F7FAFF"/>
        <SolidColorBrush x:Key="CardBorder" Color="#D9E6FF"/>

        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,16,16"/>
        </Style>

        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <!-- alias so {StaticResource Label} works -->
        <Style x:Key="Label" TargetType="TextBlock" BasedOn="{StaticResource FormLabel}" />
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: form -->
        <Border Style="{StaticResource Card}" Grid.Column="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Doctor" Style="{StaticResource Label}"/>
                <ComboBox  Grid.Row="0" Grid.Column="1"
                           ItemsSource="{Binding Doctors}"
                           DisplayMemberPath="Name"
                           SelectedItem="{Binding SelectedDoctor, Mode=TwoWay}"
                           Width="220"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Patient" Style="{StaticResource Label}"/>
                <ComboBox  Grid.Row="1" Grid.Column="1"
                           ItemsSource="{Binding Patients}"
                           DisplayMemberPath="Name"
                           SelectedItem="{Binding SelectedPatient, Mode=TwoWay}"
                           Width="220"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Date" Style="{StaticResource Label}"/>
                <DatePicker Grid.Row="2" Grid.Column="1"
                            SelectedDate="{Binding Date, Mode=TwoWay}"
                            SelectedDateFormat="Short"
                            IsTodayHighlighted="True"
                            Width="220"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Time" Style="{StaticResource Label}"/>
                <ComboBox  Grid.Row="3" Grid.Column="1"
                           ItemsSource="{Binding TimeSlots}"
                           SelectedItem="{Binding Time, Mode=TwoWay}"
                           Width="220"/>

                <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,12,0,0">
                    <Button Content="New"    Command="{Binding NewCommand}"   Width="70" Margin="0,0,10,0"/>
                    <Button Content="Save"   Command="{Binding SaveCommand}"  Width="70" Margin="0,0,10,0"/>
                    <Button Content="Delete" Command="{Binding DeleteCommand}" Width="70"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Right: upcoming list -->
        <Border Style="{StaticResource Card}" Grid.Column="1">
            <DataGrid AutoGenerateColumns="False"
                      ItemsSource="{Binding Upcoming}"
                      SelectedItem="{Binding Selected, Mode=TwoWay}"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date"   Binding="{Binding Date, StringFormat=\{0:yyyy-MM-dd\}}" Width="120"/>
                    <DataGridTextColumn Header="Time"   Binding="{Binding Time}" Width="90"/>
                    <DataGridTextColumn Header="Doctor"  Binding="{Binding Doctor}" Width="*"/>
                    <DataGridTextColumn Header="Patient" Binding="{Binding Patient}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
